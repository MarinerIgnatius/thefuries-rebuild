# Cloudflare Pages redirect rules
#
# Astro is configured with build.format: 'directory', so every page
# lives at /slug/index.html and the canonical URL has a trailing slash.
# Cloudflare Pages handles the non-slash → slash redirect natively.
# Do NOT add /slug/ → /slug rewrites here — they create an infinite loop.

# 404 fallback (Cloudflare Pages serves 404.html automatically)
